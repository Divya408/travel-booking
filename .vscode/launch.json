{
    "version": "2.0.0",
    "configurations": [
        {
            "name": "Launch local site",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}/travel-booking"
        }
    ]
}